import { Layout, AddEdit } from 'components/delegation';

export default Add;

function Add() {
    return (
        <Layout>
            <h4>Add Delegation Account</h4>
            <AddEdit />
        </Layout>
    );
}